package leetcode.kotlin.dailydecember2023.elementappearingmorethan25insortedarray

class Solution2 {
  fun findSpecialInteger(arr: IntArray): Int {
    val len = arr.size
    var possibles = listOf(len / 4, len / 2, 3 * len / 4)
    TODO()
  }
}